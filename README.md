#### Create a zip file of app
```shell
cd sampleapp-v1
zip -r sampleapp-v1.zip .
```

#### Create a zip file of app
```shell
cd sampleapp-v2
zip -r sampleapp-v2.zip .
```

Upload the zip files to s3 bucket to be used for codedeploy
